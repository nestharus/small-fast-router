/*
 * Lombok plugin configuration
 */

// Configure Lombok for all projects that have the lombok plugin applied
allprojects {
    plugins.withId('io.freefair.lombok') {
        lombok {
            version = versions.lombok
        }
    }
}
